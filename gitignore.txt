# === Операционная система (Windows) ===
# Игнорируем системные файлы Windows, которые создаются автоматически
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msm
*.msp
*.lnk

# === Редакторы кода (JetBrains) ===
# Игнорируем папку с настройками проектов PyCharm, IDEA и т.д.
.idea/
# Игнорируем временные и служебные файлы IDE
*.iws
*.iml
*.ipr

# === Наш проект (SceneClick) ===
# Никогда не загружаем сам установщик в репозиторий
*.exe
*.msi

# Если используешь VS Code, раскомментируй следующие строки
# .vscode/
# *.code-workspace
